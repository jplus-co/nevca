<ul class="{{ wrap ? "tease-list flex-wrap" : "tease-list" }}">
  {% for item in props %}
    <li class="{{ flex_basis
      ? 'tease-list__item flex-basis-' ~ flex_basis
      : 'tease-list__item'}}">
      <a class="tease-list__link" href="#0">{{ loop.index }}</a>
    </li>
  {% endfor %}
</ul>
